<%- include('includes/head.ejs') %>

<% if(users.length > 0) { %>
<ul>
  <% for (const user of users) { %>
  <li><%= user %></li>
  <% } %>
</ul>
<% } else { %>
<h3>No Users to display...</h3>
<% } %>

<%- include('includes/end.ejs') %>